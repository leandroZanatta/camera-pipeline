# Incluir arquivos de configuração e documentação
include pyproject.toml
include README.md

# Incluir recursivamente o código Python
recursive-include camera_pipeline *.py

# Incluir recursivamente os fontes C e CMakeLists
recursive-include c_src *

# Incluir a biblioteca compilada no local onde o CMake a instala
# (Ajuste o caminho/extensão conforme necessário para diferentes OS)
# (Comentado pois scikit-build deve lidar com o .so agora)
# graft camera_pipeline/core/camera_processor/lib

# (Comentado pois scikit-build deve lidar com o .so agora)
# graft camera_pipeline/core/camera_processor/lib 