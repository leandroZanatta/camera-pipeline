# Incluir recursivamente o diretório c_src - ESSENCIAL PARA SDIST
graft c_src

# Incluir arquivos de configuração/metadados necessários para o build
include pyproject.toml
include setup.py
include README.md # Opcional no sdist, mas comum
include LICENSE   # Opcional no sdist, mas comum

# Excluir diretório de build C da sdist
prune c_src/build

# Não incluir o código python explicitamente aqui, 
# setuptools deve encontrá-lo via 'packages' em pyproject.toml.
# graft camera_pipeline 